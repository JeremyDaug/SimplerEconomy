# TODO

## Open

- Write version cuts into `docs/Overview.md`.
- Fill in `Unit` and `TechTree`. The tech tree is not a beaker pile. How it sits beside decentralized innovation is still open.
- Fill in `PlayState::advance_turn`.
- Load a scenario through `init`.
- Decide how sentiment connects to other behavior.
- Decide how stored AMV moves, aside from the zero dead-zone.
