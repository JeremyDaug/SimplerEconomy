
use std::collections::{HashMap, HashSet};

use crate::game::factuals::Factuals;

// [original Process code unchanged]

#[cfg(test)]
mod tests {
    // FULL process test code (all helpers + check_factor + do_process_leg_should + do_process_should) moved from lib.rs
}
